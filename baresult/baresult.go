// Package baresult is the sister package of `result` that implements the same
// monadic functionality for error handling in Go but instead of introducing a
// dedicated wrapper type, it works directly on `(T, error)` tuples.
package baresult
